<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <!--
        ЛР №1 (docs/lab-01-indexes.md, задание 25): индексы, которые полностью дублируют
        уже существующие. Поиск по этим колонкам продолжают обслуживать UNIQUE-ограничения
        и первичные ключи, а каждая вставка перестаёт обновлять лишнюю структуру.
    -->
    <changeSet id="010-drop-duplicate-indexes" author="taskmanager" runInTransaction="false">
        <!-- те же колонки, что у UNIQUE-ограничений -->
        <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_users_email</sql>           <!-- = users_email_key -->
        <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_users_username</sql>        <!-- = users_username_key -->
        <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_tags_name</sql>             <!-- = tags_name_key -->
        <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_api_keys_key_hash</sql>     <!-- = api_keys_key_hash_key -->
        <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_idempotency_keys_key</sql>  <!-- = idempotency_keys_key_key -->

        <!-- левый префикс составного первичного ключа -->
        <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_task_tags_task_id</sql>     <!-- pk_task_tags (task_id, tag_id) -->
        <sql>DROP INDEX CONCURRENTLY IF EXISTS idx_user_projects_user_id</sql> <!-- pk_user_projects (user_id, project_id) -->

        <rollback>
            <sql>CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_user_projects_user_id ON user_projects (user_id)</sql>
            <sql>CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_task_tags_task_id ON task_tags (task_id)</sql>
            <sql>CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_idempotency_keys_key ON idempotency_keys (key)</sql>
            <sql>CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_api_keys_key_hash ON api_keys (key_hash)</sql>
            <sql>CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_tags_name ON tags (name)</sql>
            <sql>CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_users_username ON users (username)</sql>
            <sql>CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_users_email ON users (email)</sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
